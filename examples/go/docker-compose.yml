services:
  go-app:
    image: golang:1.21.7
    container_name: go-app
    restart: unless-stopped
    working_dir: /app
    volumes:
      - ./src:/app
    command: ["tail", "-f", "/dev/null"]
    networks:
      - shared-network
    depends_on:
      - redis

  # 示例：连接 Redis
  redis:
    external: true
    name: redis

networks:
  shared-network:
    external: true