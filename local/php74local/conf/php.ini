[PHP]
; 这里可以根据需要精简或覆盖默认 php.ini，
; 初始内容来源于 php.ini-production，保持与原示例一致。

; 为避免冗长，这里建议直接挂载本机自定义 php.ini，
; 或继续沿用当前文件的默认设置。

; 关键选项示例：
display_errors = Off
log_errors = On
memory_limit = 256M
post_max_size = 32M
upload_max_filesize = 16M

